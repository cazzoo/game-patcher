<Window x:Class="ModEditor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ModEditor"
        mc:Ignorable="d"
        Title="RF ModEditor" Height="350" Width="525">

    <DockPanel>
        <Menu IsMainMenu="True" DockPanel.Dock="Top">
            <MenuItem Header="_File">
                <MenuItem Header="_New..." />
                <Separator />
                <MenuItem Header="_Open..." />
                <Separator />
                <MenuItem Header="_Save" />
                <MenuItem Header="_Save As..." />
                <Separator />
                <MenuItem Header="_Exit" Click="MenuItem_Click" />
            </MenuItem>
            <MenuItem Header="About" />
        </Menu>
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Margin="10px" HorizontalAlignment="Left" VerticalAlignment="Bottom" Width="75" Click="MenuItem_Click" />
            <Button Content="Open mod file" Margin="10px" HorizontalAlignment="Left" VerticalAlignment="Bottom" Width="75" Click="Button_Click" />
            <Button Content="Save mod file" Margin="10px" HorizontalAlignment="Left" VerticalAlignment="Bottom" Width="75" Click="Button_Click" />
        </StackPanel>
        <TabControl>
            <TabItem Header="Mod data">
                <WrapPanel>
                    <StackPanel Orientation="Horizontal" Margin="5">
                        <Label Content="Name" Target="{Binding ElementName=modName}" />
                        <TextBox Name="modName" MinWidth="150" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="5">
                        <Label Content="Version" Target="{Binding ElementName=modVersion}" />
                        <TextBox IsReadOnly="True" Name="modVersion" MinWidth="100" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="5">
                        <Label Content="Mandatory ?" Target="{Binding ElementName=modMandatory}" />
                        <CheckBox Name="modMandatory" VerticalAlignment="Center" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="5">
                        <Label Content="FolderName" Target="{Binding ElementName=modFolderName}" />
                        <TextBox Name="modFolderName" MinWidth="150" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="5">
                        <Label Content="Author" Target="{Binding ElementName=modAuthor}" />
                        <TextBox Name="modAuthor" MinWidth="150" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="5">
                        <Label Content="Password" Target="{Binding ElementName=modPassword}" />
                        <TextBox Name="modPassword" MinWidth="150" ToolTip="Optional field">
                        </TextBox>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="5">
                        <Label Content="Contact" Target="{Binding ElementName=modContact}" />
                        <TextBox Name="modContact" MinWidth="150" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="5">
                        <Label Content="Categories" Target="{Binding ElementName=modCategories}" />
                        <TextBox Name="modCategories" MinWidth="150" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="5">
                        <Label Content="Tags" Target="{Binding ElementName=modTags}" />
                        <TextBox Name="modTags" MinWidth="150" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="5">
                        <Image Height="50" Width="50" AllowDrop="True" Source="Resources/MissingImage.png" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="5">
                        <Label Content="CreationDate" Target="{Binding ElementName=modCreationDate}" />
                        <TextBox IsReadOnly="True" Name="modCreationDate" MinWidth="150" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="5">
                        <Label Content="UpdateDate" Target="{Binding ElementName=modUpdateDate}" />
                        <TextBox IsReadOnly="True" Name="modUpdateDate" MinWidth="150" />
                    </StackPanel>
                </WrapPanel>
            </TabItem>
            <TabItem Header="Mod files">
                <DataGrid x:Name="dataGrid"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="True"
                  CanUserSortColumns="True"
                  AlternatingRowBackground="WhiteSmoke"
                  Grid.Column="0" Grid.Row="0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Binding="{Binding FilePath}" Header="File path" />
                        <DataGridTextColumn Binding="{Binding FileName}" Header="File name" />
                        <DataGridTextColumn Binding="{Binding FileSize}" Header="File size (octet)" />
                        <DataGridCheckBoxColumn Binding="{Binding Deletable}" Header="Deletable ?" />
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
        </TabControl>
    </DockPanel>
</Window>